<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cakecal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/chocolate</item>
        <item name="colorButtonNormal">@color/chocolate</item>
        <item name="colorOnBackground">@color/caramel</item>
    </style>

    <style name="Theme.App" parent="Theme.MaterialComponents.Light">
        <item name="bottomNavigationStyle">@style/Widget.MaterialComponents.BottomNavigationView.Colored</item>
        <item name="colorPrimary">@color/dust</item>
        <item name="colorOnPrimary">@color/caramel</item>
    </style>
</resources>